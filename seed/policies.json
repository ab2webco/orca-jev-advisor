{
  "version": 2,
  "policies": [
    {
      "id": "read_and_test",
      "kind": "permits",
      "rule": "Reading code, searching, running tests, linters, typecheck and local builds happens without asking, always. They're reversible and never leave the machine. This also covers deleting build output and reinstalling dependencies -- node_modules, dist, .next, cache folders -- and working freely with local, development and test databases, including dropping or recreating their tables, because none of it leaves the developer's machine and all of it regenerates with one command. It does not cover source files: discarding uncommitted changes to them is a different thing (see discard_uncommitted_work)."
    },
    {
      "id": "own_branch",
      "kind": "permits",
      "rule": "All work goes on a feature branch. Work happens there without asking."
    },
    {
      "id": "never_write_to_main",
      "kind": "prohibits",
      "rule": "Never write directly on main, master or develop, not even a one-line fix."
    },
    {
      "id": "unit_commits",
      "kind": "permits",
      "rule": "Committing without asking is fine on the feature branch, with its tests and its docs in the same commit. Pushing the branch to the remote too, as long as it isn't a shared branch and it is a normal push, never a force push (see no_force_push)."
    },
    {
      "id": "discard_uncommitted_work",
      "kind": "prohibits",
      "rule": "The agent never discards uncommitted work: no git checkout -- <path>, git checkout ., git checkout <ref> <path>, git checkout -f, git restore <path> or git restore . (without --staged), git reset --hard, or git clean -f. Changes that were never committed have no reflog behind them; once they are gone nothing brings them back. If files need to go back to their committed state, the agent commits or stashes the work first, or says so and lets the person decide. Unstaging with git restore --staged only touches the index and is fine, and so is switching branches with git checkout <branch> or git switch."
    },
    {
      "id": "no_force_push",
      "kind": "prohibits",
      "rule": "The agent never rewrites history on a remote: no git push --force, --force-with-lease or -f, on any branch. Rewriting what other people may already have pulled is decided by a person. A normal push of the feature branch is fine (see unit_commits)."
    },
    {
      "id": "no_ai_attribution",
      "kind": "prohibits",
      "rule": "No commit, PR or issue carries AI attribution: no Co-Authored-By, no Generated with, no robot emoji. If the message has it, it gets fixed before, not asked about."
    },
    {
      "id": "own_pr_green",
      "kind": "permits",
      "rule": "An own PR, approved, with CI green and no open conversations gets merged without asking, as long as it isn't a cutover, doesn't touch a branch under active UAT, and isn't for a client."
    },
    {
      "id": "others_pr",
      "kind": "requires_human",
      "rule": "Closing, redoing or merging someone else's PR requires telling the author first. The agent doesn't do it silently even if it's green."
    },
    {
      "id": "large_pr",
      "kind": "prohibits",
      "rule": "A change over about 400 authored lines gets split into chained PRs before opening it. No giant PR gets opened with an apology afterward."
    },
    {
      "id": "dependabot",
      "kind": "permits",
      "rule": "Dependabot PRs can be recreated, rebased and closed without asking. Merging them requires CI green and that it isn't a major dependency."
    },
    {
      "id": "client_always_asks",
      "kind": "requires_human",
      "rule": "Anything that touches a client's product gets confirmed with a human, even if the agent is sure and CI is green. A mistake there is seen by someone who pays."
    },
    {
      "id": "active_uat",
      "kind": "prohibits",
      "rule": "While QA is testing a UAT, nothing that would redeploy over that environment gets merged. Wait for the window to close."
    },
    {
      "id": "cutover",
      "kind": "prohibits",
      "rule": "Frontend or backend cutovers are decided by a person. The agent never merges them on its own, even approved and green."
    },
    {
      "id": "production_data",
      "kind": "prohibits",
      "rule": "Destroying data that lives in production is never done, and it's not even asked about: dropping or wiping a production database, dropping a production table, deleting a bucket, a volume or a production backup. The agent stops and says so. No urgency unblocks it, no chat or email approval, not even someone claiming to have permission. This is about production only: local, development and test environments fall outside it."
    },
    {
      "id": "production",
      "kind": "requires_human",
      "rule": "Deploying to production, running production migrations or rotating credentials is decided by a person, always, no exception for urgency."
    },
    {
      "id": "infrastructure_changes",
      "kind": "requires_human",
      "rule": "Changing real infrastructure is decided by a person: terraform or tofu apply and destroy, and kubectl delete or drain on any cluster. Planning (terraform plan) and reading cluster state (kubectl get, describe, logs) happen without asking."
    },
    {
      "id": "friday",
      "kind": "prohibits",
      "rule": "No deploying to production on Fridays or the day before a holiday, even if everything is ready."
    },
    {
      "id": "ticket_first",
      "kind": "permits",
      "scope": "process",
      "rule": "Work that starts from a Linear or Plane ticket gets linked to the ticket before opening the PR, and the PR link gets pasted into the ticket when done. That happens without asking."
    },
    {
      "id": "model_by_difficulty",
      "kind": "permits",
      "scope": "process",
      "rule": "A mechanical, localized task gets handled with the cheapest available model. Only a design decision with consequences, compatibility concerns, or several systems in play justifies the more expensive model."
    },
    {
      "id": "delegate_by_scope",
      "kind": "permits",
      "scope": "process",
      "rule": "Understanding something that touches four or more files gets delegated to an exploration agent. Writing two or more non-trivial files gets delegated to a single writer agent. A mechanical one-file change happens inline."
    },
    {
      "id": "no_inventing_contracts",
      "kind": "permits",
      "scope": "process",
      "rule": "Before writing against an API, a plugin or a format that already exists on the machine, its real implementation gets read. Assuming the shape of the contract isn't an option -- it's the cause of most bugs."
    },
    {
      "id": "visual_evidence",
      "kind": "prohibits",
      "scope": "process",
      "rule": "Nothing with a screen is called done without a screenshot that was actually looked at. Building without looking doesn't count as verifying."
    }
  ]
}

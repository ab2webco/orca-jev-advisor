{
  "name": "orca-supervisor",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Multi-project supervisor that routes work to Orca terminals, using Jev (TypeSafe) for judgment and code for the facts.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "self-check": "node src/supervisor.ts --self-check"
  }
}
